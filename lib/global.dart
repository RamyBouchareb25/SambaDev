import 'package:flutter/material.dart';

const Color primaryColor = Color(0xFF0C46C4);
const Color accentColor = Color(0xFF387DBF);
const Gradient gradient = LinearGradient(
  colors: [Color(0xFF9BBEDF), Color.fromARGB(102, 56, 126, 191)],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);
